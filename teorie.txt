  caractere:
  char c; - 1 byte
  cin.get(c)-citeste un singur carcter dar poate citi space si caractere invizibile pe ecran;
  sizeof(char)-afiseaza cat spatiu ocupa in bytes;

  short int :
  short--> 2 bytes = 16 biti
  2^16 valori posibile;

  int --> 4 bytes = 32 biti
  2^32 valori posibile;

  long long --> 8 bytes = 64 biti  
  

// 1sec=200.000.000 operatii
// include  <climits> ca sa folosesc INT_MAX/MIN;
// 1 mil intregi->>4 mb;
/*la two pointers:
daca am restrictii naspa in problema, la inceput-elimina-le!!!!!
la pbinfo_memorie006-curs4
1.timpul => brut decent
2.memoria => 100mb
3.tipul de date(nr prea mari) => nr sunt mici